import React from 'react';

import '../../scss/playground.scss';

export default class Playground extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <main>
        There is nothing here yet because stenno is lazy.
      </main>
    );
  }
}